# r18_comic プロジェクト

## Git 設定

**必ずこのリポジトリでは以下のユーザーでコミット・pushすること:**

- user.name: `aicmasaki0827`
- user.email: `250306790+aicmasaki0827@users.noreply.github.com`
- remote: `https://github.com/aicmasaki0827/r18_comic.git`

git操作前に必ず確認:
```bash
git config user.name   # → aicmasaki0827 であること
git config user.email  # → 250306790+aicmasaki0827@users.noreply.github.com であること
```

別のアカウント（例: TITMasakiSaito / masaki.saito@takumi-it.co.jp）でコミットしないこと。
